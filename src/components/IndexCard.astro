---
const { cardImage, cardTitle } = Astro.props;
---

<a href={`/destinations?search=${cardTitle}`}>
    <div class="flex flex-col items-center border-2 border-gray-400 p-5 rounded-lg">
        <img class="rounded-full mb-4" src={`../../../assets/images/${cardImage}`} alt={cardImage} width="40px" height="100%"/>
        <h3 class="text-lg font-semibold text-indigo-700">{cardTitle}</h3>
    </div>
</a>
